import { SonaDirective } from './sona.directive';

describe('SonaDirective', () => {
  it('should create an instance', () => {
    const directive = new SonaDirective();
    expect(directive).toBeTruthy();
  });
});
